using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeActivatePrefab : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // foreach (GameObject child in gameObject.transform)
        //     {
        //         child.SetActive(false);
        //         Debug.Log(child.name + " was turned off");
        //     }
        // Renderer parentRenderer2 = new Renderer();
        // parentRenderer2 = gameObject.GetComponent<Renderer>();

    }

    // Update is called once per frame
    void Update()
    {
        // Renderer parentRenderer2 = new Renderer();
        // parentRenderer2 = gameObject.GetComponent<Renderer>();

        // if(parentRenderer2.enabled == true)
        // {
        //     foreach (GameObject child in gameObject.transform)
        //     {
        //         child.SetActive(true);
        //     }
        // }
        // else
        // {
        //     foreach (GameObject child in gameObject.transform)
        //     {
        //         child.SetActive(false);
        //     }
        // }

    }
}
